# TASK

## 1. 목표

금단현상 알림이 화면의 긴 문장이 화면 width를 넘어갈 때 공백문자 기준으로 자연스럽게 개행되도록 수정한다.

## 2. 현재 상황

- `WithdrawalInfoScreen`에서 `withdrawalMessages` 문구가 날짜별 안내에 표시된다.
- 문구가 길 경우 React Native 기본 줄바꿈에만 의존하고 있다.

## 3. 현재 문제

긴 문장이 화면 폭을 넘거나 어색하게 줄바꿈될 수 있다. 사용자는 문장의 공백문자 기준으로, width를 넘는 문장에 한해서 개행되길 원한다.

## 4. 플랫폼 영향

- Android: 금단현상 알림이 화면의 긴 문구가 공백 기준으로 줄바꿈된다.
- Web: 동일한 텍스트 개행 로직이 적용된다.
- 공통 로직: 알림 예약/저장 로직은 변경하지 않는다.

## 5. 관련 파일

- `src/screens/WithdrawalInfoScreen.tsx`
- `TASK.md`
- `RESULT.md`

## 6. 원인 가설

화면 폭과 카드 padding을 고려한 명시적 개행 처리가 없어, 긴 문구가 플랫폼별 기본 줄바꿈 방식에 맡겨져 있다.

## 7. 수정 요구사항

- 화면 폭 기준으로 한 줄에 들어갈 수 있는 대략적인 글자 수를 계산한다.
- 문장이 해당 글자 수를 넘는 경우 공백 단위로 줄바꿈한다.
- 단어 내부는 자르지 않는다.
- 이미 짧은 문장은 변경하지 않는다.

## 8. 금지사항

- 알림 예약 로직 변경 금지
- 서버/DB 추가 금지
- CSS/SCSS 추가 금지

## 9. 검증 방법

- `npm.cmd run typecheck`
- `npm.cmd run export:web`

## 10. 작업 결과 기록 방식

작업 후 `RESULT.md`에 변경 파일, 변경 내용, 검증 결과를 기록한다.
